# ぼぶる

トレードの計画・記録・分析・ルールブックを1つにまとめた、単一HTMLのWebアプリ。
依存ライブラリなし・ビルド不要。`index.html` をブラウザで開くだけで動きます。

## 機能

- **ホーム** — 口座の状態（残高・有効証拠金・含み損益・証拠金・当日実現損益・前日比・保有中の合計予定損失）、振り返り待ち、本日の損益/R/回数、連勝連敗、今日の環境認識の要約、昨日の取引
  - MT5 を繋いでいれば、**残高系は全部自動**です。人が入れるのは「口座開始残高」だけ（一度きり）
  - **Fintokei の資金管理ルール（日次損失・最大損失・失格ライン・残り許容額）はぼぶるでは判定しません。** 正本は Fintokei なので、そちらで確認して下さい
- **振り返り待ち** — MT5 から入った決済済みで、根拠・感情・反省がまだ空のものを一覧に。ホームの件数から、その場で書けます。事実は入っているので、判断と振り返りだけ書けば終わります
- **朝やろ** — ホームか環境タブのボタン1つで、**前日の取引を同期 → 今日の市場データを取得 → 環境認識を保存**まで回します
  - iPhone のショートカットに `https://<公開先>/index.html#morning` を開く動作を1つ置き、名前を「朝やろ」にすると **「ボブ、朝やろ」で Siri から呼べます**
  - Mac が OFF でもスリープでも動きます。MT5 に触るのは VPS 側だけなので → [agent/README.md](agent/README.md)
  - 各段の結果が画面に残ります。取得先が未接続の段は「飛ばした」と出ます（黙って成功したふりはしません）
  - 一段目に**昨日の取引**（件数・実現損益・勝敗・振り返り未記入件数）。未記入があればそこから振り返り待ちへ飛べます
- **環境** — その日の Morning Brief。日足/4H/1H/15M の方向、現在価格と EMA 10/75/200、前日高安・週足高安・直近高安・主要サポレジ、アジア/ロンドン/ニューヨークのセッション別高安、経済指標（時刻・重要度）、今日の見方（ロング優勢/ショート優勢/様子見）、Bull / Bear / No Trade シナリオ、要約と市場材料メモ。1日1件で、その日に保存したトレードへ自動で紐付きます
  - **朝分析** ボタンで市場データを取り込めます。取り込んだ内容は編集してから保存します
  - 一覧は「9/8 Morning Brief」の形で、その日の件数・損益・勝敗・**朝と同方向 / 逆**の件数が並びます
  - 取得先は「未接続」（手入力）・「中継（OHLC）」・「デモ」。差し替え口は `marketDataProvider` にまとめてあります → [docs/phase1-morning-analysis.md](docs/phase1-morning-analysis.md)
  - 中継は素の足（OHLC）を返すだけで、EMA・ATR・方向・前日高安・週足高安・セッション高安は**この端末で計算**します。業者のAPIキーは中継に置き、端末には置きません
  - 本数が足りない足では値を出さず「データなし」にします。シナリオと今日の見方は相場観なので自動では作りません
  - セッション時間は `Asia/Tokyo` / `Europe/London` / `America/New_York` のタイムゾーンで計算するので、サマータイムに自動で追従します
- **取引同期（MT5）** — 環境タブから、MT5 の取引履歴を取り込みます → [docs/phase2-morning-auto.md](docs/phase2-morning-auto.md)
  - 取り込むのは **MT5 から実際に取れる事実だけ**。エントリー根拠・感情・相場観・パターンは空のまま残ります
  - 一意キーは deal ticket と position ID。同じ取引が二重登録されません
  - 手で直した値は次の同期で潰しません。食い違いは黙って消さずに報告します
  - VPS が無くても、エージェントの JSON を貼り付けて取り込めます
  - 手で立てた計画と同じに見える取引は**紐付け候補**として出ます。紐付けると1つの記録にまとまり、カードに「計画 vs 実際」（ロット / Entry / SL / TP→Exit / RR→実績R）が出ます。自動では確定しません
- **口座情報の同期** — Balance / Equity / Floating P/L / Margin / Free Margin / 証拠金維持率を MT5 から受け取ります
  - 当日実現損益・前日比・エントリー時の口座残高は、口座情報と約定から**計算**します（手入力しません）
- **計画** — エントリー前に価格・SL・TP・ロットを入力すると、予定損失/利益・リスク率・RR・推奨ロットをライブ計算（内部リスク率は自分のルールです）
- **トレード** — 保有中 / 計画中（下書き）/ 決済済み / 振り返り待ち / 取消 の一覧と編集、決済登録。銘柄・方向の隣に RR を表示します
- **成長** — 期間別の勝率・PF・純R・最大DDなどの集計、累積損益/累積R/残高推移のグラフ、パターン別・環境別の分類集計
  - **朝の見方との関係**（朝と同じ方向 / 逆の方向）と **朝の4時間足との関係**（沿った / 逆らった）
  - **時間軸** — 曜日別 / セッション別（アジア・ロンドン・NY・重複）/ エントリー時間帯別 / 保有時間別。曜日と時間帯は端末の時計、セッションは各市場の時計で数えます
  - **MAE / MFE** — 保有中の最大逆行・最大順行と、SL までの距離を 1 とした比率、決済後4時間の伸び。1分足で計算できたものは正式、15分足に落ちたものは「15分足ベース推定値」と明記します
- **ルール** — フォルダ分けしたルールブック（変更履歴つき）と、仮説を検証する「研究」機能
- **設定** — 口座通貨・現在残高の出どころ・MT5 の口座情報（読むだけ）・内部リスク基準・為替レート・中継の接続先・テーマ

## 口座通貨

XAUUSD のような USD建ての銘柄は損益が USD で出ます。口座通貨が JPY のときは
USDJPY を自動取得して、予定損失・予定利益・リスク率・推奨ロットを口座通貨で計算します。

- レートは無料の公開レート（キー不要）を10分キャッシュで取得します。**更新は1日1回**なので、日中の変動は反映されません
- 取得できないときは手動レート、それも無ければ 1 で計算し、画面にその旨を出します
- レートはエントリー時の値をトレード記録に焼き込むので、あとでレートが動いても過去のリスク率と R は揺れません
- 詳しくは [docs/fx-rate.md](docs/fx-rate.md)

## MT5 と VPS

朝は iPhone から使うことが多いので、MT5 に触る部分は Mac ではなく**常時動いている場所（Windows VPS）**に置きます。

```
[Windows VPS]                     [中継]                    [iPhone / PC]
 MT5端末（ログイン済み）             Cloudflare Workers        ぼぶる
   └ mt5_agent.py  ──POST署名──▶  /api/deals  ──GET──▶  取引同期
                                  /api/ohlc   ──GET──▶  朝分析
```

**MT5 のログイン情報は VPS から一歩も出ません。** 中継にもぼぶるにも渡らず、置く場所もありません。
エージェントはログイン済みの端末につなぐだけなので、スクリプトも設定ファイルもパスワードを持ちません。

建て方・秘密の置き場所・取れる項目と取れない項目 → [agent/README.md](agent/README.md)

## 何を人が入れて、何が自動か

考え方は「システムから取れる数字は人間に入力させない」。

| 人が入れる | 自動（MT5・市場データ・計算） |
|---|---|
| エントリー根拠・パターン・トリガー | 銘柄 / 方向 / エントリー価格・時刻 / 決済価格・時刻 / ロット |
| 感情・ルール遵守度 | 実現損益 / 手数料 / スワップ / SL / TP / 契約サイズ |
| 反省・学び・任意メモ | 残高 / 有効証拠金 / 含み損益 / 必要証拠金 / 余剰証拠金 / 証拠金維持率 |
| 口座開始残高（一度きり） | 当日実現損益 / 前日比 / 保有中の合計予定損失 / エントリー時の口座残高 |
| — | 現在価格 / EMA / ATR / 各足の方向 / 前日高安 / 週足高安 / セッション高安 / USDJPY・DXY・米10年債 |
| — | 曜日 / セッション / エントリー時間帯 / 保有時間 / MAE / MFE |

取れなかったものは**推測で埋めず空のまま**残します。
Fintokei の資金管理ルールの判定（日次損失・最大損失・失格ライン・3% Risk Rule）は Fintokei 側で行い、ぼぶるは持ちません。

## データの扱い

- 記録は `localStorage` にこの端末だけで保存されます（ぼぶる自身はサーバーを持ちません）
  - 取引同期と朝分析だけが中継を経由します。中継が持つのは MT5 の約定と市場データで、トレードの記録や環境認識は送りません
- スクリーンショットは別ストアに保存され、同期・バックアップの対象外です
- 端末間の移動は設定タブの**同期コード**（丸ごと上書き）で行います
  - 取り込みは丸ごと上書きなので、アプリを更新したときは**両方の端末で一度リロードしてから**同期して下さい
- JSONバックアップ / 復元、CSV書き出しに対応

## 使い方

`index.html` を開くだけ。同じファイルがPCとスマホの両方に合わせて表示を変えます。

- **スマホ**（横幅900px未満）— 下部タブ。ホーム画面に追加するとフルスクリーンで使えます
- **PC**（900px以上）— 左サイドバー。本文が広くなり、グラフも大きくなります
- **広い画面**（1180px以上）— ホームは指標が横1列＋環境認識とFintokeiが左右に、成長はグラフと集計表が2列に並びます

## 開発

回帰テスト（任意）。実ブラウザで保存・同期・復元・計算・レイアウトを確認します。

```
npm i -D playwright && npx playwright install chromium
node tests/run.mjs
```

アプリ本体は依存ライブラリなしのままです。playwright はテストを走らせるときだけ必要です。
